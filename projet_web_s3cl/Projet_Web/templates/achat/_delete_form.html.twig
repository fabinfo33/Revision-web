<form class="d-inline-block float-right" method="post" action="{{ path('achat_delete', {'codeAchat': achat.codeAchat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ achat.codeAchat) }}">
    <button class="btn btn-danger btn-lg btn-sm">Supprimer</button>
</form>
