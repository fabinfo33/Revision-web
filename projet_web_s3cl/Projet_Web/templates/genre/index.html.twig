{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <div class="row">
        {% for genre in genres %}
            <div class="col-sm-3">
                <div class="card text-center">
                    <div class="card-body">
                        <h5 class="card-title">{{ genre.LibelleAbrege }}</h5>
                        <a href="{{ path('genre_show', {'codeGenre': genre.codeGenre}) }}" class="btn btn-primary">Voir</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>


{% endblock %}
